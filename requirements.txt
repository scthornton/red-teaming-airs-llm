# Prisma AIRS Red Teaming - Python Dependencies

# NOTE: pan-aisecurity (the AI Runtime Security SDK) is deliberately NOT listed.
# Nothing in this repo imports it - every app calls the Scan API over plain HTTP
# with `requests`. Keeping the pin only broke installs, because pan-aisecurity
# requires Python >= 3.10 while this repo advertises 3.8+.

# Web framework for test application
flask>=3.0.0

# HTTP requests
requests>=2.31.0

# Optional: Real LLM for testing
# openai>=1.3.0
# anthropic>=0.7.0

# Environment variables
python-dotenv>=1.0.0
